<% cache [action_name, I18n.locale, resource.site, resource] do %>
	<%= render_page_title(resource.title) %>
	<%= render_slideshow_for(resource, :plugin => "slideshow") %>
	<%= render_html_text(resource.body) %>
<% end %>